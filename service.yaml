apiVersion: v1
kind: Service
metadata:
  name: my-country-service
spec:
  type: NodePort
  selector:
    app: country-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
      protocol: TCP
